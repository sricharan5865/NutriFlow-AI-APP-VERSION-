import { CapacitorConfig
} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.surya.nutriflow',
  appName: 'nutriflow-ai',
  webDir: 'dist' // Use bundled assets from dist folder
  // Server config removed - app will use local bundled files
};

export default config;